{
  "google": {
    "_comment": "Prefer putting secrets in .env (GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET). You may also set them here directly.",
    "clientId": "",
    "clientSecret": "",
    "redirectUri": "http://localhost:3000/oauth2callback"
  },
  "calendars": [
    { "id": "primary", "label": "Main", "role": "events" },
    {
      "id": "xxxx@group.calendar.google.com",
      "label": "Work location",
      "role": "location"
    }
  ],
  "weather": {
    "latitude": 35.6895,
    "longitude": 139.6917,
    "locationName": "Tokyo",
    "timezone": "Asia/Tokyo"
  },
  "_stocks_comment": "Optional: powers the /portrait dashboard ticker via Twelve Data (twelvedata.com). Get a free API key and put it in .env as TWELVE_DATA_API_KEY (preferred) or in apiKey below. Max 8 symbols. For non-US markets add mic (mic_code) or exchange/country per symbol; symbols are grouped by market and fetched one request per market. Japan (Tokyo) = 4-digit code + mic XJPX (e.g. 7203 = Toyota) and usually needs a paid plan. label is the on-screen name; chg is percent change vs previous close.",
  "stocks": {
    "enabled": true,
    "refreshSeconds": 300,
    "apiKey": "",
    "symbols": [
      { "symbol": "7203", "mic": "XJPX", "label": "トヨタ" },
      { "symbol": "9984", "mic": "XJPX", "label": "SBG" },
      { "symbol": "AAPL", "label": "AAPL" },
      { "symbol": "NVDA", "label": "NVDA" }
    ]
  },
  "server": {
    "port": 3000,
    "calendarRefreshSeconds": 60,
    "weatherRefreshSeconds": 1800
  },
  "allDayLocationKeywords": [
    "home",
    "remote",
    "office",
    "WFH",
    "on-site",
    "business trip"
  ],
  "display": {
    "brightness": {
      "method": "software",
      "transitionMinutes": 30,
      "schedule": [
        { "from": "06:00", "level": 100 },
        { "from": "20:00", "level": 50 },
        { "from": "23:00", "level": 15 }
      ]
    },
    "view": {
      "schedule": [{ "from": "00:00", "mode": "full" }],
      "night": { "from": "23:00", "to": "06:00" }
    }
  },
  "remote": {
    "enabled": true,
    "backgrounds": ["bg.jpg"],
    "screenPower": {
      "off": "xset dpms force off",
      "on": "xset dpms force on"
    }
  }
}
